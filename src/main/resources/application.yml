spring:
  security:
    oauth2:
      client:
        registration:
          google:
            client-id: CLIENT_ID
            client-secret: CLIENT_SECRET
    saml2:
      relyingparty:
        registration:
          okta-saml:
            identityprovider:
              metadata-uri: METADATA-URI
